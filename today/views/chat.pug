extends ./base


block content

    section#chat
        h1 Chat

        ul
            li === Start of Chat ===

        form()
            input(type="text" name="message" required)
            input(type="submit" value="chat")
